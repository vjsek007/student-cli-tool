name: Package on Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # <--- CRITICAL: Allows the robot to see all Git Tags

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install build tools
        run: python -m pip install build hatch-vcs --user

      - name: Build package
	shell: bash
        run: python -m build --sdist --wheel

      - name: List dist contents
        run: ls -la dist/

      - name: Upload assets to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload ${{ github.event.release.tag_name }} \
            dist/*.whl \
            dist/*.tar.gz \
            --clobber
